# MCP Service Dockerfile - VULNERABLE Version

FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir \
    fastapi==0.109.0 \
    uvicorn==0.27.0 \
    httpx==0.26.0 \
    redis==5.0.1 \
    pydantic==2.5.3 \
    prometheus-client==0.19.0

COPY main.py .

# NOTE: No health check in vulnerable version

EXPOSE 8002

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8002"]
